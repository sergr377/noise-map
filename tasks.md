# Статус, решения и незакрытые задачи

Снимок состояния проекта. README — документация для читателя, CLAUDE.md — заметки
для работы с кодом, здесь — что сделано, почему сделано именно так и что осталось.

---

## Статус

Работает и проверено вживую в браузере:

| Часть | Состояние |
|---|---|
| Расчёт CNOSSOS-EU с рельефом | работает, проверен на реперных точках |
| HTTP-слой: очередь, кэш, SSE | работает, сквозной тест гоняется в CI |
| Отмена расчёта, лимиты частоты и одновременных подписок | работает, проверено на живой задаче |
| Замок каталога задачи: один прогон на точку | работает, проверен на живых процессах |
| Предварительная карта всей площади за ~2 мин | работает, проверено в браузере |
| Живой рендер кадрами | работает, по умолчанию выключен |
| Посчитанные области затенены на карте | работает, проверено в браузере |
| Период и точка в ссылке | работает |
| Фронтенд: карта, изофоны, периоды | работает |
| Поиск по адресу | работает |
| Ссылка на точку (`?lat=&lon=`) | работает |
| Прогрев кэша | работает |
| Прогрев целого города по плану тайлов | работает, Краснодар посчитан целиком |
| Ж/д шум | **не работает**, ветка остановлена |
| Деплой | образ собран, контейнер работает, расчёт доходит до конца |

Ключевые замеры:

- холодный расчёт **354–1609 с** на радиусе показа 750 м, пик JVM **2203 МБ**
  (на прежних 500 м было 178–582 с и 1836 МБ);
- повторный клик — **~5 мс** из кэша;
- выхлоп **1.6–2.2 МБ** (388–518 КБ по проводу в gzip);
- диапазон уровней 32.5…82.5 дБ(A), тихая зона в **16–19 м** от громкой в разных
  прогонах — экранирование зданиями работает, карта не вырождается в «расстояние
  до дороги».

---

## Принятые архитектурные решения

### Расчёт

**Движок — NoiseModelling, запускаемый отдельным процессом.** Своя реализация
CNOSSOS-EU — это человеко-месяцы и гарантированные ошибки в физике. Отдельный
процесс вместо линковки библиотеки: движок под GPLv3, и так копилефт не
распространяется на код проекта.

**Трафик оценивается по классу дороги (WG-AEN), а не по Яндекс.Пробкам.** У пробок
нет публичного API с числовыми данными, и главное — «загруженность» это про
скорость, а не про поток: связь немонотонная, в глухом заторе шума меньше, чем на
свободной магистрали. Карта по баллу пробок местами была бы обратна реальности.

**Приёмники ограничены зоной показа, источники берутся шире.** Считать весь
квадрат выгрузки ради заметно меньшей зоны показа — втрое лишней работы. Дало
главное ускорение: 549 → 182 с.

**Показывается круг, но считается описанный квадрат.** `Delaunay_Grid` берёт от
переданного `fence` только его envelope — это видно в исходнике блока
(`setMainEnvelope`), — поэтому сетку приёмников кругом не ограничить. Круг
вырезается в конце, пересечением готовых изофон с диском: 21% приёмников
считаются впустую, зато граница ровно там, где обещано, и не зависит от того,
что именно движок сделал с fence. Удалять приёмники и треугольники вне круга
перед распространением дешевле, но `Create_Isosurface` потребляет таблицу
`TRIANGLES` целиком, и её правка — отдельный риск ради 21%.

**Рельеф включён по умолчанию,** хотя стоит +60% времени. Обосновано измерением:
9.6% площади меняет диапазон, средний уровень падает на 1.2 дБ, площадь тихих
зон растёт — рельеф добавляет экранирование. Без него модель молча считает город
плоской плитой.

**Периоды D/E/N/DEN считаются за один прогон.** Затухание вдоль пути считается
один раз, эмиссия пересчитывается под трафик периода — переключатель времени
суток достался бесплатно.

**Изофоны склеиваются в базе (`ST_Union`), а не на клиенте.** 3005 фичей → 41,
3.6 МБ → 0.64 МБ, ценой 1.3 с на фоне 70 с расчёта.

**Ж/д задумана вторым проходом распространения с энергетическим сложением,** а не
добавлением источников в первый. Дорожный путь проверен, и включение ж/д не должно
иметь возможности изменить результат без неё.

### Сервис

**Сервер запускает CLI подпроцессом, а не дублирует пайплайн.** `run-job.mjs`
остаётся единственным источником правды; сервер добавляет только очередь, кэш и
трансляцию прогресса.

**Кэш по сетке ~100 м, расчёт ведётся по центру ячейки.** Ключ и центр обязаны
совпадать, иначе второй кликнувший получает карту, центрированную по первому.

**Готовый результат отдаётся на любую точку внутри своего диска.** Ячейка кэша
~100 м, диск результата 750 м — без этого правила клик по затенённой области
почти всегда попадал мимо посчитанной ячейки и запускал четверть часа работы ради
карты, которая уже есть. Отдаётся тот из подходящих дисков, в который клик попал
глубже. Честно это потому, что диск однороден: источники и здания берутся шире
показа ровно на `maxSrcDist`, поэтому у кромки расчёт такой же полный, как в
центре. Плата — карта центрирована не в точке клика, о чём интерфейс говорит
прямо.

**Посчитанные области показываются все сразу, а не под курсором.** Подсказка под
курсором отвечала только про ту точку, куда навели: узнать о готовой области
можно было, лишь случайно попав в неё мышью. Теперь карта запрашивает всё
посчитанное в рамке в полтора экрана, когда камера остановилась, и затеняет эти
диски. Через провод идут центры и радиусы, а не контуры, а перед отрисовкой диски
объединяются в одну фигуру (`polygon-clipping`, +8.6 КБ gzip): рендерер заливает
полигоны по отдельности, поэтому на пересечениях прозрачность складывалась, а
стилем это не лечится — ни режима наложения, ни прозрачности слоя в API нет. Для этого рядом с
результатом пишется файл с координатами — ключ кэша это хеш, и место из него не
достать, — а записи прежних параметров расчёта отсеиваются сверкой `cacheKey`:
затенить такую значило бы обещать мгновенный ответ там, где клик запустит расчёт
заново.

**Ключ кэша включает параметры расчёта.** Плата: любая правка радиуса, дифракции
или рельефа обесценивает весь кэш и требует повторного прогрева. Взамен —
невозможность отдать результат, посчитанный по другим настройкам.

**Прогрев города идёт по ранжированному плану, а не по голой решётке.**
Треугольная решётка кругов показа по границам из OSM — это только форма; полезным
план делает порядок. Тайл получает те здания, для которых он ближайший центр, и
план пишется по убыванию, поэтому `--share` режет префиксом, а прерванный прогрев
успевает посчитать населённую часть города, а не случайный угол: по Краснодару 254
тайла закрывают 95% застройки против 632 на весь округ, причём у 155 из этих 632
нет ни одного своего здания, а у 85 — ни одной дороги с трафиком. План привязан к
`JOB_PARAMS.radius` — половине ключа кэша, — поэтому `prewarm.mjs` сверяет его с
`/api/health` и отказывается стартовать при расхождении. Оценка времени —
подгонка по 14 законченным прогонам (`0.073·здания + 0.087·дороги`, R² = 0.71), а
не обещание.

**База H2 удаляется дважды, и это два разных требования.** Перед прогоном —
правильность: `Import_OSM` дописывает в таблицы с фиксированными именами, и база
от убитого прогона подмешалась бы в следующий. После удачного — место: 11 МБ на
разреженном тайле и 46 МБ на плотном, втрое-вдесятеро больше всего остального в
каталоге. Батч на 254 тайла оставил бы 4–13 ГБ, оставил 2.4 ГБ. Упавший прогон
свою базу сохраняет: без неё нечем ответить на вопрос, что именно движок
произвёл.

**Один одновременный расчёт.** Распространение и так занимает все ядра; второй
параллельный делает медленнее оба.

**Отмена снимает ожидающего, а не убивает задачу.** Запросы к одной ячейке
разделяют расчёт; будь отмена прямым убийством, один клиент обрывал бы работу
другому. Расчёт останавливается, когда ушёл последний ожидающий. Закрытая
вкладка при этом не отменяет ничего: браузер не обещает запрос при выгрузке, а
отмена по обрыву SSE ломала бы перезагрузку страницы.

**Убивается всё дерево процессов, а не подпроцесс сервера.** Ядра и 1.8 ГБ
держит JVM в конце цепочки `run-job.mjs` → ScriptRunner → java. Отсюда
`detached` и сигнал группе под Linux, `taskkill /T` под Windows и снятие задач
по SIGINT/SIGTERM — за `detached` расчёт перестаёт умирать вместе с терминалом.

**Каталог задачи занят одним прогоном.** Каталог назван по точке, и имена
файлов внутри фиксированы, так что два прогона одной точки писали в одну базу H2
и перемешивали её таблицы — наблюдалось 2026-08-19 после того, как расчёт пережил
убитый сервер. Теперь прогон занимает `run.lock` в каталоге, а второй отказывается
стартовать. Лок с признаком жизни: владелец касается его каждые 15 с, брошенным он
считается по молчанию дольше минуты или по исчезнувшему pid — снятие лока на
выходе ненадёжно, под Windows `taskkill /F` не даёт отработать ничему. Каталог
оставлен общим на точку, а не уникальным на прогон, ради кэша выгрузки OSM и
растра рельефа.

**Карта показывается по ходу расчёта, а не только в конце.** Движок пишет
уровни непрерывно (`NoiseMapWriter` отдельным потоком, батчами, с коммитом), и
второе соединение к встроенной H2 их видит — измерено до того, как что-либо
писалось. Поэтому кадры строятся из уже посчитанного, а не имитируются.

**Цена кадров не видна на стенных часах.** Перемерено на чистой машине, Тверская,
три прогона подряд по одному извлечению: с кадрами 912 с (7 кадров, сумма 73.6 с)
и 966 с (8 кадров, сумма 102.2 с), без кадров — 933 с, ровно между ними. Собственное
время потока кадров — 8–11% расчёта, как и обещает ограничитель в девять раз, но
это один поток из двенадцати: в стенных часах его доля — секунды, и она тонет в
разбросе между одинаковыми прогонами (54 с). Прежние 11% из испорченного замера
были не ценой расчёта, а верхней границей на время самого потока.

**Ждущему показывается предварительная карта, а не кадры.** Кадр точен, но
покрывает четверть круга: распространение идёт ячейками, и на Тверской первый
кадр пришёл на 83-й секунде с 6% приёмников, а половина площади набралась к
десятой минуте. Целая карта полезнее точного огрызка, поэтому перед точным
проходом считается предварительный — та же сетка приёмников, те же здания,
источники в пределах 75 м, без рельефа, — и он даёт всю площадь за 38 с
распространения вместо 926. Кадры выключены по умолчанию (`PARTIAL_INTERVAL_MS=0`)
и подменять готовую карту огрызком не должны: на экране это выглядело бы как
разрушение карты.

Дальность источников — единственный настоящий рычаг цены. `maxArea` им не
является: восьмикратный предел площади треугольника убрал 1.3% приёмников, потому
что сетку Делоне на городской застройке задают контуры зданий. Рельеф в
предпросмотре не окупается — стоит лишних 63 с и не приближает карту к итогу
(10.2% площади мимо полосы против 9.9% без него): без рельефа уровни завышаются,
отброшенные дальние источники их занижают, ошибки частично гасят друг друга.

Дальность выбрана в 75 м, а не 150, сознательным разменом: 150 м дают 97 с и
занижение на 1.1 дБ (9.9% площади мимо полосы), 75 м — 38 с и 2.7 дБ (19.0%).
Точный ответ всё равно в четверти часа, поэтому целая карта через минуту после
клика полезнее более верной через две с половиной; заметнее всего огрубление во
дворах, которые предпросмотр рисует тише, чем они есть.

Расплата названа в интерфейсе теми же числами, так что `PREVIEW_SRC_DIST` и
подпись в `App.tsx` меняются только вместе. В кэш предпросмотр не попадает.
Второй проход стоит +6% к холодному расчёту на Тверской; платит только тот, кто
ждёт, — прогрев кэша идёт без предпросмотра. Сетка на оба прохода одна не случайно: геометрия
изофон совпадает, и точный расчёт перекрашивает карту на месте, а не
перерисовывает её.

**Кадр и финальный результат идут через одну функцию склейки.** Разойдись эти
два пути, кадр показывал бы не то, что окажется в ответе.

**Кадр — украшение, а не часть расчёта.** Он считается в том же JVM, поэтому
пауза между кадрами равна девятикратной цене предыдущего: накладные расходы
ограничены примерно десятой частью времени, что бы ни случилось с ценой кадра.
Любая ошибка внутри кадра логируется и глотается — уронить расчёт она не может.
Кадры не попадают в кэш: там обязан лежать только законченный результат.

**Подписки на прогресс считаются по занятости, а не по частоте.** SSE-соединение
живёт весь расчёт и держит сокет, слушателя и таймер, а стоило одного токена
корзины, поэтому сотня висящих потоков укладывалась в любой бюджет. Число
одновременных подписок с адреса ограничено отдельно (`STREAM_LIMIT_PER_IP`, по
умолчанию 6), лишняя получает 429; место возвращается, как только соединение
закрылось — в том числе когда клиент исчез, не попрощавшись.

**Бюджет расчётов тратится только на запуск новой работы.** Кэш и подключение к
идущей задаче не считаются: ограничение защищает очередь, а не чтение — иначе
демка перестала бы быть демкой. Геокодер вынесен в отдельную корзину, потому что
тратит не наши ядра, а общую квоту ключа.

**Loopback не ограничивается.** `prewarm.mjs` ходит через тот же API, и оператор,
греющий собственный кэш, — не та нагрузка, от которой лимит защищает.

**Один контейнер: сервер отдаёт и API, и собранный фронтенд.** Отдельный
статик-хостинг не нужен, деплой — один процесс.

**Геокодер проксируется сервером, карта — нет.** У ключа JS API есть ограничение
по referer, поэтому он безопасен в бандле; у геокодерного ограничения нет, и в
браузер он попасть не должен.

### Интерфейс

**Палитра Coloring Noise вместо национальных стандартов.** Выбрана по измерениям,
не по вкусу: у DIN 18005-2 три инверсии светлоты и два самых громких диапазона
различаются на ΔE 7.7 при нормальном зрении — то есть уровни, которые важнее всего
различать, он рисует почти одинаково.

**Отступ карты под панель измеряется, а не выводится из брейкпоинта.** Высота
панели зависит от содержимого, положение переключается между нижней панелью и
боковой колонкой; дублировать это правило в JS значило бы держать две копии.

**Спросить о результате можно, не запуская расчёт.** `GET /api/noise?lat=&lon=`
отвечает без побочных эффектов; на нём держится подсветка уже посчитанных мест
под курсором. Запрос уходит по остановке курсора, а не на каждое движение, и
ответы запоминаются — иначе подсказка съедала бы лимит частоты на разглядывание
карты.

**Счётчик времени считает возраст задачи, а не длительность своего ожидания.**
Каждый отчёт сервера переставляет точку отсчёта, между отчётами счётчик идёт
локально. Иначе перезагрузка страницы или подключение к чужому расчёту начинали
отсчёт с нуля и врали на минуты; проверено на живой задаче — свежая вкладка
показала 111 с при возрасте задачи 129 с на момент следующего замера.

**Область расчёта показывается кругом: пунктирным под курсором, сплошным во
время счёта.** Пунктир — предложение, сплошная — то, что происходит; разница
читается без подписи. Сплошной круг рисуется вокруг центра ячейки кэша, а не
вокруг точки клика: расчёт идёт именно там, и до половины ячейки — это
расхождение, которое пользователь имеет право видеть.

**Прогресс не обгоняет подтверждённое сервером** больше чем на несколько процентов.
Ощущение живости даёт локальный счётчик секунд, а не выдуманный прогресс.

**Сквозная проверка HTTP-слоя гоняется в CI на подменённом движке.**
`smoke-api.mjs` годами оставался ручным, потому что запускал настоящий расчёт:
Java, дистрибутив на 148 МБ вне git, живой Overpass, минуты всех ядер — ничего
этого на бесплатном раннере нет. Но HTTP-слой из этого и не состоит.
`RUN_JOB_SCRIPT=scripts/fake-job.mjs` подставляет заглушку с той же командной
строкой, тем же протоколом маркеров и теми же файлами — она стоит **под**
очередью, кэшем, SSE и лимитером, а не вместо них, поэтому все 25 проверок
настоящие. Заглушка пишет в `jobs/stub_*` и подмешивает признак подмены в ключ
кэша, так что выдуманная карта недостижима для сервера с настоящим движком;
подмена объявляется в логе старта и в `GET /api/health`. Про акустику это не
говорит ничего — `sanity-check` и `compare-runs` остаются ручными, там нужен
глаз на числа.

---

## Оставшиеся задачи

### Блокирует деплой

- [x] **Собрать и проверить Docker-образ.** Требует включения виртуализации в BIOS
      и прав администратора — программно недостижимо. Непроверенным остаётся всё:
      сборка, загрузка дистрибутива на этапе build, `npm ci --omit=dev`, запуск
      под Linux. Два риска сняты статически: байткод под Java 11 (JRE 17 подойдёт),
      юниксовый лаунчер в дистрибутиве есть.
      **Сделано** (2026-09-02): образ собран за **5 мин 51 с**, вышло **999.6 МБ**
      распакованными и 322.8 МБ сжатыми — JRE 184 МБ, дистрибутив движка 173 МБ,
      `node_modules` 12 МБ после `--omit=dev`, фронтенд 260 КБ. Контейнер
      поднимается `healthy`, ноль перезапусков, без OOM; `/api/health` отвечает
      без поля `engine`; кэш доехал в том — 570 файлов. Холодный расчёт в
      контейнере доходит до конца: 45.0490,39.1230 при радиусе 1000,
      `maxSrcDist` 500 и `reflOrder` 1 — **14 мин 03 с** от извлечения OSM до
      изофон (21:47:13 → 22:01:16), 2.67 МБ результата, пик памяти контейнера
      **1495 МиБ** при лимите 4 ГБ. Это параметры по умолчанию `run-job.mjs`, а
      не серверные `JOB_PARAMS`, и по радиусу, дальности источников и отражениям
      они тяжелее — с замерами 354–1609 с на радиусе 750 это число сравнивать
      нельзя. Три статических предположения подтвердились: JRE 17 подошёл,
      юниксовый лаунчер на месте, `npm ci --omit=dev` дал работоспособный
      рантайм.
      **Где формулировка задачи оказалась неполной:** сборка падала не из-за
      окружения. Каталог `shared/` не копировался ни в одну из двух стадий, хотя
      `config.ts:4` и `queue.ts:17`–`:18` импортируют его относительным путём;
      `tsc` валился с тремя `TS2307` и кодом возврата 1, то есть сборка вставала
      на `RUN npm run build:server`. Замерено на воссозданном контексте сборки:
      без каталога — код 1, с каталогом — 0. В Dockerfile дописаны две строки
      `COPY shared ./shared`, по одной в каждую стадию.
      **Закрыто после ретрая в `dem.mjs`:** холодная задача, запущенная
      *через сервер* и на серверных `JOB_PARAMS` (`r750s1100`), в контейнере
      прошла до конца — 45.1204,38.8506, **1 мин 29 с**: извлечение, рельеф,
      предпросмотр, изофоны 44 КБ, база H2 подметена (а это делается только
      после успеха), результат лёг в кэш под тем же id, что вернул
      `POST /api/noise`, и probe-маршрут теперь отвечает по этой точке
      `cached: true`. Плитка разряженная — 127 КБ извлечения, — так что о
      плотной застройке это число не говорит ничего; проверялся путь, а не
      производительность. Плотная застройка проверена отдельно — см. уточнение
      к задаче про хостинг ниже: Тверская в контейнере проходит за 17.8 мин при
      пике 1630 МиБ.

- [x] **DNS в контейнере мигает, и загрузка рельефа этого не переживает.**
      Расчёт, запущенный через сервер, умер на стадии рельефа:
      `getaddrinfo EAI_AGAIN s3.amazonaws.com`, уже после того как Overpass
      отдал 7.2 МБ. Это не разовая случайность — замер из того же контейнера
      дал один отказ (`ENOTFOUND`) на шесть попыток подряд. `fetchTile` в
      `dem.mjs:27` ходит за тайлом одним `fetch` без ретрая, а все тайлы
      собираются через `Promise.all`, поэтому единственный сбой DNS роняет всю
      задачу. Overpass в своё время получил четыре зеркала, ротацию,
      `Retry-After` и бюджет; рельеф не получил ничего, потому что на хосте DNS
      не мигает. Лечится ретраем с отступом вокруг `fetchTile`: тайлов на диск
      750 м считанные единицы, цена ретрая нулевая. До этого демка будет терять
      часть кликов на пустом месте, и именно этот отказ выглядит для
      пользователя как «сломалось всё».
      **Сделано:** `fetchTile` экспортирован и получил четыре попытки с той же
      жёваной лестницей, что у Overpass, — `backoffDelay` переиспользован, а не
      написан заново, только с базой 300 мс вместо 5 с: тайл это пара сотен
      килобайт с CDN, и вся лестница укладывается в ~2 с против задачи, которая
      стоит минуты. Ретраится то, что может измениться: сетевой сбой, 429, 5xx,
      а заодно битый PNG — обрыв на середине скачивания выглядит именно так.
      404 и 403 не ретраятся вовсе (`tileVerdict`): тайла, которого в бакете
      нет, на десятой попытке не появится. Текст ошибки теперь несёт код из
      `err.cause` — без него `fetch failed` не говорит ничего.
      **Чем проверено.** Восемь тестов в `test/dem.test.mjs` на подставных сети
      и сне (что ретраится, что нет, что лестница растёт и что половина каждого
      шага — джиттер); `npm test` — 56 проходят. Затем на живой сети
      контейнера: 20 запросов тайла, **один пришёл с тем самым
      `fetch failed (EAI_AGAIN)` и стоил 229 мс** вместо упавшей задачи, ноль
      окончательных отказов. Образ пересобран (14 с на прогретых слоях).
- [ ] **Выбрать хостинг.** Нужно ≥3 ГБ памяти; бесплатные тарифы с 256–512 МБ и
      shared CPU не подходят. Решить, включать ли `CACHE_ONLY=1` — тогда демка
      отдаёт только прогретые места и не пускает многоминутные расчёты.
      **Уточнение после первой сборки:** лимит контейнера нельзя выбирать по
      пику процесса. JVM берёт кучу как `MaxRAMPercentage=25` от лимита — при
      `mem_limit: 4g` это ровно 1 ГБ (`MaxHeapSize = 1073741824`, замерено
      внутри контейнера), тогда как на хосте пик JVM был 2203 МБ.
      Краснодарская плитка в гигабайт уложилась, но плотная застройка упрётся в
      `OutOfMemoryError` внутри JVM, а не в лимит снаружи — и поднимать надо
      будет лимит, потому что куча считается от него. То есть 4 ГБ здесь не
      «запас к 2.2 ГБ», а «1 ГБ кучи»; хосту с 3 ГБ придётся либо задавать
      `-XX:MaxRAMPercentage` явно, либо соглашаться на меньшую кучу.
      **Замерено на плотной застройке (2026-09-03):** Тверская на серверных
      параметрах в контейнере **проходит** — 17.8 мин, пик **1630 МиБ** из 4096,
      ни одного `OutOfMemoryError`, база H2 подметена. Основной проход: 33 891
      приёмник, 7 836 источников, 3 716 зданий — то есть это действительно
      худший из имеющихся случаев, и он совпал с прежним замером на хосте
      (34 101 приёмник), так что под Linux пайплайн считает то же самое.
      Важное следствие: **прежние 2203 МБ завышали потребность.** На хосте с
      32 ГБ куча по умолчанию — гигабайты, и JVM просто ленивее собирает мусор;
      с кучей в 1 ГБ рабочий набор укладывается и расчёт доходит до конца при
      1630 МиБ всего контейнера. То есть **4 ГБ лимита проверены на худшем
      случае**. Что *не* проверено — меньшие лимиты: 3 ГБ дают 768 МБ кучи, и
      уложится ли в них та же Тверская, неизвестно. Прогон на 3 ГБ — ещё 18
      минут, и его стоит сделать до выбора тарифа, если разница в цене между
      3 и 4 ГБ имеет значение.
      **Решение (2026-09-04): арендовать VDS у Selectel**, тариф 4-8-80 —
      4 выделенных ядра, 8 ГБ, 80 ГБ. Восемь гигабайт, а не четыре, именно
      потому, что 4 ГБ — это лимит контейнера: на машине с ровно четырьмя
      системе не остаётся ничего и OOM придёт снаружи Docker, где замеры выше
      уже ничего не значат. Ядра нужны выделенные — под непрерывной
      пятнадцатиминутной нагрузкой общие троттлятся ровно в середине расчёта.
      **Хостинг на домашней машине проверен и отброшен**; подробности в README,
      раздел «Что не подошло: туннель с домашней машины». Коротко: канал за
      CGNAT, проброс портов невозможен в принципе, а туннель не работает, потому
      что `cloudflared` остался только на QUIC, а оператор связи QUIC режет.
      Прогрев при этом остаётся домашним: 6 ядер против 4 и бесплатно, а кэш
      переносится обычными файлами — это и есть связка «считать дома, отдавать
      с сервера».
- [ ] **Добавить боевой домен в ограничения ключа** в кабинете Яндекса, рядом с
      `localhost`. Без этого карта на сайте отдаст `403 Invalid api key`.
      **Отменяется**, если раньше будет закрыта эпика «Своя картографическая
      основа» ниже: ключа карт там не остаётся вовсе.

### Своя картографическая основа

Эпика: убрать Яндекс из проекта целиком. Подложка — MapLibre GL на своих
векторных тайлах, поиск по адресу — на OSM-геокодере. Ветка `own-map-stack`.

**Зачем.** Бесплатный лимит JS API — около сотни загрузок в сутки, и это не
лишние запросы, а ровно посетители: карта грузится при каждом открытии страницы.
Оптимизировать тут нечего, потолок аудитории равен лимиту. Второй мотив не про
счётчик: бесплатное использование JS API ограничено открытыми некоммерческими
проектами, а сохранение полученных через API данных запрещено — то есть кэш
геокодера, который напрашивается сам собой, условиями закрыт.

**Почему это возможно.** Яндекс не поставляет ни одного числа, участвующего в
расчёте: пробки не используются (причина выше в этом файле), изофоны считаются
из OSM. Подложка и геокодер — всё, и оба заменяются, не трогая модель.

**Выгода помимо лимита.** Экранирование считается по зданиям OSM, а нарисованы
сейчас яндексовы. Тихая зона за домом, которого на подложке нет, читается как
ошибка расчёта. На OSM-подложке карта и модель начинают говорить об одних домах.

**Решения, принятые до начала.** Тайлы — Краснодарский край: он совпадает с
реальным покрытием расчётов (`plans/krasnodar.json` и почти весь `jobs/`).
Следствие, которое нельзя забыть: **дефолтный центр карты переезжает из Москвы
в Краснодар** — иначе сервис открывается на пустой подложке. Геокодер входит в
эту же ветку, а не в следующую: у Яндекса, как и у Google, есть требование
показывать полученные через API данные на их же карте, поэтому сменить подложку
и оставить их геокодер — значит поменять одно нарушение условий на другое.

- [x] **Range-запросы в `serveStatic`.** Блокировало всё остальное: PMTiles
      читается кусками по смещениям и без `Range` не работает вовсе.
      **Сделано.** Тайлы вынесены в `TILES_DIR` и раздаются `serveTiles` — тем же
      процессом, без nginx перед ним. Отката на `index.html` там нет намеренно:
      он нужен `dist-web`, чтобы глубокая ссылка пережила перезагрузку, а здесь
      отдавал бы HTML с кодом 200 на месте недостающего `.pmtiles`.
      Проверено вживую шестью проверками, они же добавлены в `smoke-api.mjs`:
      `bytes=10-19` отдаёт именно этот кусок, `bytes=-5` хвост, `bytes=99999-`
      даёт `416` с реальным размером, отсутствующий тайл — `404 application/json`,
      `%2e%2e%2f` из каталога не выпускает.
      **Найдено по ходу:** `max-age` в неделю на пересобираемом `.pmtiles` — это
      не просто устаревший файл, а склейка кусков старой сборки с кусками новой,
      потому что читается он диапазонами. Теперь глифы `immutable` навсегда
      (диапазон кодов начертания — один файл раз и навсегда), стиль и тайлы
      ревалидируются по `ETag`.
- [x] **Подложка: MapLibre GL вместо ymaps3.**
      **Сделано.** `ymaps.ts`, `ymaps-global.d.ts` и `@yandex/ymaps3-types`
      удалены, `MapCanvas` переписан императивно. `mapErrors.ts` остался: причина
      его существования сменилась, но не исчезла — статический импорт из `App`
      по-прежнему утащил бы в первый чанк всю библиотеку карты.
      Изофоны едут одним GeoJSON-источником с цветом в свойствах объекта; порядок
      объектов внутри коллекции заменил `zIndex`, `palette.ts` остался
      единственной таблицей цветов. Цикл ретраев вокруг `map.bounds` (40 попыток
      по 50 мс) удалён — у MapLibre есть `load`. `zoomForDisc` не тронута.
      **Оценка бандла оказалась неверной.** Ожидался рост вдвое; на деле главный
      чанк **уменьшился** — 208 КБ против 226, — а MapLibre целиком уехал в
      ленивые чанки (~268 КБ в gzip), которые грузятся только вместе с картой.
      **Мелочь, которую легко пропустить:** у `.centre-dot` убраны отрицательные
      отступы. Маркер MapLibre центрируется по элементу сам, прежний движок клал
      в точку левый верхний угол.
- [x] **Стиль и глифы — свои.**
      **Сделано**, `basemap/style.json`. Схема своя, а не позаимствованная
      целиком: подложка тут фон под изофоны с прозрачностью 0.55, отсюда три
      правила — всё ненасыщенное, дороги белые и читаются сквозь заливку, здания
      с z14. Последнее не косметика: это те самые здания OSM, по которым считалось
      экранирование, а на чужой подложке тихая зона за отсутствующим домом
      читалась как ошибка расчёта. Подписи по `{name}` — в OSM по России он уже
      русский.
      **Спрайт не понадобился вовсе:** иконок в стиле нет, подписи только текстом.
      Одним скачиваемым куском меньше.
      Глифы собраны и проверены: 512 файлов, Noto Sans Regular и Bold. Medium в
      архиве openmaptiles/fonts нет — места подписаны Bold.
- [x] **Собрать тайлы Краснодарского края.**
      **Сделано.** `basemap.pmtiles` — **155.7 МБ, 4 мин 35 с** на 11 потоках.
      Оценка «300–600 МБ и десятки минут» оказалась завышенной вдвое-впятеро.
      Диск в пике не был узким местом: карта узлов ушла в память
      (`PLANETILER_STORAGE=ram` при 32 ГБ на машине), и от 10 ГБ свободных
      осталось 8 ГБ, из которых 1.7 ГБ — скачанные источники, которые можно
      удалить. Перед сборкой освобождено 756 МБ удалением баз H2 из `jobs/`;
      выгрузки OSM и растры рельефа не тронуты — они переиспользуются.
      **Проверено в браузере:** Краснодар рисуется с улицами, зданиями и русскими
      подписями; изофоны ложатся поверх и идут вдоль проезжей части; затенение
      посчитанных областей на месте; консоль чистая.
      **Найдено по ходу:** MapLibre вычисляет адрес своего воркера в рантайме из
      `import.meta.url`, поэтому ни один сборщик не видит его статически — файл не
      попадал в сборку, карта просила `/assets/maplibre-gl-worker.mjs`, получала
      `index.html` и падала на проверке MIME-типа. Молча: пустое поле и ни слова в
      интерфейсе. Лечится импортом через `?worker&url` и `setWorkerUrl`.
      **И ещё:** Planetiler напомнил, что тайлы OpenMapTiles требуют видимого
      кредита «© OpenMapTiles» рядом с OSM. В атрибуции стиля его не было.
      **Грабли, найденные по дороге:** `tar` в Git Bash — GNU 1.34, zip он не
      читает вовсе и вдобавок принимает «C:\...» за имя удалённого хоста;
      `unzip` есть в образе, но не в голой Windows, PowerShell наоборот. Поэтому
      появился `scripts/unzip.mjs` на zlib. И Java не читает переменные окружения
      про прокси — `build-tiles.mjs` переводит их в системные свойства JVM сам,
      иначе шаг загрузки выглядит как недоступный Geofabrik.
- [x] **Поиск по адресу — на OSM.**
      **Сделано**, Nominatim. Кроме суточного лимита это снимает и запрет на
      хранение ответов: ODbL кэшировать разрешает, требуя лишь атрибуции. Кэш в
      памяти на 2000 записей, ключ нормализован по регистру, пробелам и запятым.
      Очередь на весь процесс с паузой 1.1 с — публичный инстанс просит не чаще
      запроса в секунду и банит за нарушение. Это защита чужого сервера от нас, в
      отличие от корзины лимитера, которая защищает нас от посетителя.
      **Не предусмотрели заранее:** у дома Nominatim кладёт в `name` голый номер,
      и список результатов выходил «176, 176, 176» — три одинаковых строки на три
      разных места. Имя собирается из улицы и дома.
      Проверено вживую через прокси: улица находится, дом даёт `precision=exact` и
      читаемое «Красная улица, 176», повтор того же запроса с другими пробелами и
      запятыми в сеть не уходит (1316 мс против 292 мс).
- [x] **Сборка и деплой без ключей.**
      **Сделано.** `VITE_YANDEX_API_KEY` ушёл из `Dockerfile`,
      `docker-compose.yml`, `.env.example`, CI и `vite-env.d.ts`;
      `YANDEX_GEOCODER_KEY` — оттуда же. Ключей в проекте не осталось ни одного.
      Подложка подключается каталогом `./tiles:ro`, а не томом: её пересобирают
      снаружи, и каталог позволяет сделать это, не трогая контейнер.
      **Осталось на владельце:** отозвать старый ключ карт в кабинете Яндекса. Он
      всё ещё в истории git (`d1db820`), но условия «ограничение по referer должно
      оставаться настроенным» больше нет — ключ ничем не используется.
- [ ] **Nominatim плохо ранжирует русские адреса в свободном запросе.** Найдено
      при проверке интерфейса и воспроизводится стабильно: на «Красная улица 40,
      Краснодар» первая пятёрка — Сочи, Динской район, Новороссийск, Приморский,
      Армавир, а самого Краснодара в ней нет. Адреса при этом верные: «Красная,
      40» в крае действительно много, и полное описание в списке их различает, —
      но города из запроса геокодер попросту не слышит.
      **Что проверено и не помогло:** порядок слов (три варианта дают тот же
      ответ), структурированный запрос `city=` + `street=` (тот же ответ),
      публичный Photon (не ответил вовсе). `viewbox` тут бесполезен — все
      найденное и так внутри края.
      **Честная оценка:** по свободному русскому запросу Яндекс ранжировал лучше.
      Это цена ухода, и её стоит либо принять, либо закрывать своим инстансом с
      настроенными весами, либо разбирать запрос на город и улицу до обращения.
      Браться стоит с замером на списке реальных запросов, а не на одном.
- [x] **Документация.**
      **Сделано.** В README раздел «Ключ Яндекс Карт» заменён на «Подложка» — как
      собирается, чем раздаётся, почему стиль свой; переписаны «Фронтенд», «Поиск
      по адресу», «Запуск», «Деплой», «Лицензии и данные», карта файлов и таблица
      лимитов. В CLAUDE.md раздел «Keys and secrets» теперь говорит, что ключей
      нет, добавлен раздел про подложку, а из заметок про фронтенд убран цикл
      ретраев вокруг `map.bounds`, которого больше не существует.
      Упоминания Яндекса остались там, где они исторические: почему не годятся
      пробки, чем условия отличались от ODbL и что старый ключ ещё в истории git.

### Интерфейс

- [ ] **Ссылка «Открыть Яндекс Карты» не обновляется** при перемещении карты и
      ведёт на исходный центр. Воспроизвести не удалось: при программном
      перемещении камеры (открытие ссылки, поиск адреса, подгонка масштаба)
      `ll` в ссылке обновляется — проверено. Остаётся случай, когда карту тащит
      мышью сам пользователь: синтетическое перетаскивание камеру не двигает,
      а снимать экран в той сессии было нельзя. Нужна проверка руками: потащить
      карту и посмотреть `ll` в ссылке. Если подтвердится — чинится подпиской на
      `onActionEnd` с обратной записью камеры в состояние.

### Что видит ждущий

Сделано: перед точным проходом считается предварительная карта всей площади
(`/api/noise/:id/preview`), интерфейс показывает её с прямой оговоркой и
заменяет итогом. Кадры живого рендера остались в коде, но по умолчанию
выключены. Подробности и грабли — в README и CLAUDE.md.

- [ ] **Кадры имеет смысл вернуть только дельтами.** Сейчас кадр перестраивает
      изофоны по всем готовым приёмникам, поэтому дорожает по ходу расчёта: на
      Тверской от 2.6 с на первом до 31.9 с на восьмом. Секундная частота, ради
      которой сетка росла бы на глазах, требует считать контуры только по новым
      треугольникам, досылать дельты и сшивать их на клиенте. Это ломает
      нынешнее свойство «кадр идёт через ту же склейку, что и итог», поэтому
      браться стоит только целиком и с замером, а не подкручивая сглаживание.
- [ ] **Хвост в 22 с не зависит от дальности.** Изофоны и склейка стоят
      столько же при любом `PREVIEW_SRC_DIST`, и в нынешних 60 с ожидания это
      уже треть. Снятие сглаживания экономит ~7 с (изофоны 15.8 → 9.9 с, склейка
      6.5 → 5.2 с) и даже уменьшает выхлоп на 7%, но контуры предпросмотра
      становятся угловатыми — замерено, отложено сознательно.

### Модель

- [ ] **Ж/д шум: проход распространения не завершается.** Диагноз есть:
      `LW_RAILWAY` выходит в третьоктавах 50 Гц – 10 кГц (72 колонки вместо 24), и
      сверху накладывается директивность поезда, считаемая на каждый путь. Дешёвого
      флага нет — свернуть третьоктавы в октавы значит изменить и работу
      директивности.
- [ ] **Трамваи невозможны** без внешнего каталога подвижного состава: в поставке
      NoiseModelling их нет.

---

## Известные ограничения, не являющиеся задачами

Это свойства выбранного подхода, а не недоделки.

- **Ключ карт остался в истории git** — внутри собранного бандла, коммит
  `d1db820`, удалён в `4da2ffd`. Решение владельца: оставить. Ключ JS API
  публичен по природе — он и сейчас виден любому посетителю в исходниках
  страницы — и защищён не секретностью, а ограничением по HTTP Referer; отсюда
  условие, на котором решение держится: **ограничение по referer должно быть
  настроено и оставаться настроенным.** Ключа геокодера в истории нет, проверено
  поиском по всем веткам: он серверный и в бандл никогда не попадал. Перевыпуск
  остаётся вариантом на случай, если ограничение придётся снять; переписывание
  истории — нет: оно сломало бы чужие клоны, а старые объекты остались бы
  доступны по SHA в кэше GitHub, то есть гарантии всё равно не дало бы.
- **Результат — оценка, а не измерение.** Геометрия, экранирование и физика
  распространения считаются точно; входной поток машин типовой для класса улицы.
  Для сравнения районов достаточно, для экспертизы — нет.
- **Холодный клик стоит минуты.** Следствие честного расчёта по CNOSSOS; лечится
  прогревом кэша, а не оптимизацией.
- **Сеточное округление иногда мажет мимо кэша:** два клика в паре метров могут
  оказаться по разные стороны границы ячейки.
- **Учитывается только автотранспорт.** Ж/д остановлена, трамваи невозможны,
  промышленность и авиация не рассматривались.
- **Данные OSM берутся с публичных зеркал.** Ставка на один сервер снята: четыре
  зеркала вместо двух, ведущее меняется от раунда к раунду, `Retry-After`
  слушается и переживает второй запрос той же задачи, пауза между раундами с
  джиттером, сломанный запрос (`400`) не объезжает зеркала, и всё вместе
  ограничено бюджетом `OVERPASS_BUDGET_MS` — отдельная попытка получает свою долю
  остатка, чтобы молчащее зеркало не съедало время работающих. Логика закрыта
  тестами: сеть, часы и `fetch` в `overpassFetch` подставляются извне. Но это
  смягчение, а не лечение, и задачей оно уже не станет: все четыре зеркала
  публичны и общи со всем миром, а настоящий выход — свой инстанс, до которого
  отсюда одна переменная (`OVERPASS_ENDPOINTS`) и вопрос железа, потому что
  полный импорт региона — десятки гигабайт и часы. Замерено: с машины разработки
  отвечают `overpass-api.de` и `maps.mail.ru`; `kumi.systems` и `private.coffee`
  через местный прокси недоступны. Из контейнера, где прокси нет, **`kumi.systems`
  ответил** — 7.2 МБ за 24.3 с, он и обслужил первый расчёт в Docker; так что
  непроверенным остаётся только `private.coffee`.
  Зеркало `overpass.osm.ch` отвергнуто: на Москву оно отдаёт пустой ответ, а не
  ошибку, потому что данные у него только по Швейцарии.
- **Состояние живёт в памяти одного процесса.** Карта задач в `queue.ts` и
  корзины лимитера в `ratelimit.ts` никуда не сохраняются. Это осознанно: один
  расчёт и так забирает все ядра, поэтому второй инстанс не ускорит ничего, а
  усложнит всё. Но следствия стоит знать заранее: перезапуск сервера теряет
  знание о запущенных задачах — сами они `detached` и продолжают считать, а
  вот подписаться на их прогресс уже нельзя, — и переход на несколько инстансов
  потребует вынести это состояние наружу, иначе лимит частоты будет считаться
  отдельно на каждом, а клик по одной точке запустит расчёт на каждом.
